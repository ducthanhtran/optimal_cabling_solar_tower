\newcommand*{\trenchcost}{c^\text{trench}}
% DATA CABLE
\newcommand*{\datacost}{c_d^\text{data}}
\newcommand*{\datatotalcost}{c_{d,(vw)}^\text{data}}
\newcommand*{\datalength}{L_d^\text{data}}
\newcommand*{\datacapacity}{M_d^\text{data}}
% SWITCHES
\newcommand*{\switchcosts}{c_{s}^\text{lcu}}

\newcommand*{\trenchtotalcost}{c_{(vw)}^\text{trench}}

\section{Problem statement}
Formally, we are given a complete, directed graph $G=(V,A)$ where $V=\{T\} \cup V_H$ with $T$ being our root solar tower and $V_H:=\{v_1,\ldots,v_{n}\}$ denotes the set of heliostats. Note that we work in a metric space $(\mathbb{R}^2,d)$, thus we denote with $v_x\in\mathbb{R}$ and $v_y \in\mathbb{R}$ both coordinates for each vertex $v\in V$. Moreover, the metric is simply the euclidean distance 
\begin{equation}
  d(v,w) := \sqrt{(v_x-w_x)^2 + (v_y-w_y)^2} \nonumber
\end{equation}
between nodes $v,w\in V$. In other words, each vertex $v\in V$ has a specified position in a 2D plane.
\emptyline
\noindent
Before a cable can be installed a trench has to be constructed by workers for which we have to pay installation costs. Let us denote these costs per meter with $\trenchcost\in\mathbb{R}^+$. In other words, the total costs of providing a trench between $v,w\in V$ is \\ 
$\trenchtotalcost := d(v,w)\cdot\trenchcost$.


\subsection{Data cable and local control units}
  We can install different types of cables which have length and connection restrictions depending on its type. For this matter let $D$ be the set of data cables with costs $\datacost\in\mathbb{R}^+$ per meter for each cable $d\in D$. We further define for readability the costs $\datatotalcost:=d(v,w)\cdot \datacost$ of installing a cable of type $d$ on the edge $(vw)\in A$. Furthermore, each data cable $d\in D$ has a length restriction $\datalength\in\mathbb{R}^+$ and a restriction on how many heliostats it can connect to, that is, $\datacapacity\in\mathbb{N}$ for $d\in D$. We denote this property by \emph{capacity restriction}.
  \emptyline
  Aside from the cables we have to build \emph{local control units} (LCU) at each heliostat. These LCUs are used for communication processing and may allow branching by e.g. installing an 8- or 16-port LCU at an heliostat. Each of these LCU has connectivity-restrictions for each cable type $d\in D$. For this matter, let $S$ the set of all LCUs where each $s\in S$ has installation costs $\switchcosts\in\mathbb{R}^+$. The connectivity or vertex-degree restrictions are given by $l_{s,d}, u_{s,d}\in\mathbb{N}$ for each LCU $s\in S$ and cable type $d\in D$. Both restrictions state that the LCU $s\in S$ has to connect at least $l_{s,d}$ cables of type $d\in D$, whereas $u_{s,d}$ is the upper bound of incident cables of type $d\in D$ at an heliosat that installed LCU $s\in S$.

\newcommand*{\powercost}{c_p^\text{power}}
\newcommand*{\powertotalcost}{c_{p,\{vw\}}^\text{power}}
\newcommand*{\powerlength}{L_p^\text{power}}
\newcommand*{\powercapacity}{M_p^\text{power}}
\subsection{Power cables}
Like the data cables, each power cable has a length and capacity restriction. Let $P$ be the set of power cables with costs $\powercost\in\mathbb{R}^+$ per meter, length restrictions $L_p^{power}\in\mathbb{R}^+$ and capacity restrictions $M_p^{power}\in\mathbb{N}$ for $p\in P$. Furthermore, we define the costs $\powertotalcost:=d(v,w)\cdot\powercost$ that are the required costs of installing a power cable $p\in P$ along the edge $\{vw\}\in E$.

In contrast to the data cables we do not to consider any type of switches for the power cables.

\subsection{Cabling problem}
For a directed tree $D(T)=(V,A)$ with root $T\in V$ we denote the unique path from the root $T$ and a vertex $v\in V$ by $path(T,v)$. Moreover, we define the following sets for $v\in V$:
\begin{itemize}
  \item $ancestors(v) := path(T,v)\setminus\{v\}$
  \item $descendants(v) := \{v\in V | \exists path(v,w)\text{ in } G\}$
\end{itemize}
\newcommand*{\treeData}{B_{\text{data}}}
\newcommand*{\treePower}{B_{\text{power}}}
\newcommand*{\assignData}{assign_{\text{data}}}
\newcommand*{\assignPower}{assign_{\text{power}}}
\begin{tcolorbox}
\begin{problem}[Cabling Problem]\label{prob:cabling_problem}
  Given a complete, directed graph $G=(V,A)$, a set of data cables $D$ and power cables $P$. Two directed, rooted trees $\treeData(T),\treePower(T)\subseteq G$ with cable assignments $\assignData:A(\treeData)\to D$ and \\
  $\assignPower: A(\treePower)\to P$, where the root is the solar tower $T\in V$, are to be constructed such that the following properties are fulfilled:
  \begin{description}
    \item[Data cable] 
    \item[Power cable] for all $a:=(u,v)\in A(\treePower)$: $\text{capacity}(\assignPower(a))$
    \item[Intersection] $\nexists e,f\in A(\treeData)\cup A(\treePower)$ that intersect with each other.
  \end{description}
  with the objective of minimizing the total cost:
  \begin{align*}
    \min\Big\{
      &\sum\limits_{(vw)\in A(\treeData)\cup A(\treePower)} \trenchtotalcost +\sum\limits_{(vw)\in A(\treeData)} \datatotalcost
      +\sum\limits_{(vw)\in A(\treePower)} \powertotalcost
    \Big\}
  \end{align*}
\end{problem}
\end{tcolorbox}

